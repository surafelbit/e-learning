{
  "name": "frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "prebuild": "npm install --no-save @oxc-parser/binding-linux-x64-gnu 2>/dev/null || true",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "@nuxtjs/supabase": "^2.0.3",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "nuxt": "^4.2.2",
    "vue": "^3.5.25",
    "vue-router": "^4.6.3"
  }
}
